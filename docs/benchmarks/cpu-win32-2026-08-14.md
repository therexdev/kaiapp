# Koinos AI model benchmark — CPU tier

Run: 2026-08-14T17:49:14.923Z · win32-x64 · AMD EPYC 9V74 80-Core Processor                 (4 threads) · 17 GB RAM · GPU: none (CPU tier)

| Model | Size | Load | Prompt tok/s | Gen tok/s |
|---|---:|---:|---:|---:|
| SmolLM2 135M Q8 (baseline tiny) | 145 MB | 0.5 s | 144 | 128 |
| Qwen2.5 0.5B Instruct Q4_K_M | 398 MB | 1.5 s | 47.0 | 43.5 |
| Llama 3.2 1B Instruct Q4_K_M | 808 MB | 2.0 s | 37.4 | 33.1 |
| SmolLM2 1.7B Instruct Q4_K_M | 1056 MB | 1.5 s | 28.2 | 23.5 |

## Sample outputs

### SmolLM2 135M Q8 (baseline tiny)

**chat:** In one sentence, what are you?

> I'm a helpful AI assistant designed to assist users in their daily lives, providing concise and informative responses.

**code:** Write a JavaScript function that reverses a string. Code only.

> Here is a JavaScript function that reverses a string:
> 
> ```javascript
> function reverseString(str) {
>     return str.split('').reverse().join('');
> }
> ```
> 
> This function works by splitting the input string into an array of characters, reversing the array using the `reverse()` method, and then joining the array back into a string using the `join()` method.
> 
> You can use this function like this:

### Qwen2.5 0.5B Instruct Q4_K_M

**chat:** In one sentence, what are you?

> I am a large language model created by Alibaba Cloud, capable of generating human-like text and performing various tasks such as language translation, summarization, and creative writing.

**code:** Write a JavaScript function that reverses a string. Code only.

> Certainly! Below is a JavaScript function that reverses a string:
> 
> ```javascript
> function reverseString(str) {
>     return str.split('').reverse().join('');
> }
> 
> // Example usage:
> const originalString = "Hello, World!";
> const reversedString = reverseString(originalString);
> console.log(reversedString); // Output: "!dlroW ,olleH"
> ```
> 
> ### Explanation:
> 1. **Split the String**: The `split('')` method splits the string into an array

### Llama 3.2 1B Instruct Q4_K_M

**chat:** In one sentence, what are you?

> I am an artificial intelligence model designed to assist and communicate with users, providing information and answering questions to the best of my knowledge based on my training data.

**code:** Write a JavaScript function that reverses a string. Code only.

> ```javascript
> function reverseString(s) {
>     return s.split("").reverse().join("");
> }
> 
> console.log(reverseString("Hello World"));
> ```

### SmolLM2 1.7B Instruct Q4_K_M

**chat:** In one sentence, what are you?

> I am a conversational AI model, designed to assist and engage in multi-round conversations, providing information and answering questions to the best of my knowledge and abilities.

**code:** Write a JavaScript function that reverses a string. Code only.

> function reverseString(str) {
>     return str.split('').reverse().join('');
> }

---
Numbers are llama-server timings (best of 2 warm runs). CPU-tier only — GPU-tier
benchmarks require real hardware and feed the same report format.
