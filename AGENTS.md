# KAI development and releases

- Primary development branch: `test`. Begin from its latest pushed commit.
- Live desktop release branch: `claude/koinos-ai-takeover-co25fw`. The repository's default branch is historical; do not base new work on it.
- The owner authorized creating and pushing updates. Work on Test by default, validate changes, and promote ready work to live without asking for routine push permission. A push and a successful installer release are different states; verify both.
- Every push to `test` runs `.github/workflows/test-release.yml` and publishes **Koinos AI Test** after verification. Keep its installer identity and updater separate. The owner explicitly wants Test to use the existing live profile, wallet, models, node-data path and ports, contributing to the live network. Preserve the live runtime app name for OS credentials and the shared Electron single-instance lock; never allow Live and Test to open the profile together. Back up configuration before a new Test version opens it.
- Promote an exact successfully built test commit using `scripts/promote-test.js` or equivalent GitHub connector operations. Merge any intervening live changes into Test and rebuild before promotion. Use a new stable version and `[release]` on the live branch. Never force-push.
- Do not publish test installers with live app IDs or live update feeds. The non-semver `test-build` release's `latest*.yml` files are compatibility pointers to the existing stable assets for legacy live updaters. Its `test*.yml` files serve only Test.
- Repository docs: `docs/TEST_RELEASES.md`. Normal verification: `npm test`; packaging additionally runs `scripts/verify-test-package.js`. Windows CI runs `scripts/verify-shared-profile.js` against separate native executables to verify mutual exclusion and live credential reuse.
- Test uses the existing network services. It is not a separate blockchain, scheduler, Docker daemon, or remote account. Do not infer authorization for wallet transfers or changes to those services from app release authorization.
- KAI companion: see docs/KAI_COMPANION.md. Keep it a view of the same Core, preserve the renderer sandbox and main-frame IPC checks, and stop microphone capture on hide/return/quit. The owner authorized access to existing app information and actions through desktop KAI, using the shared tool registry and existing privacy/approval controls. Model-proposed app mutations require per-request approval. Wallet secrets/signing, system setup and host coding workflows use their existing app screens and controls. Folder actions remain restricted to direct user commands, OS-known folders and native per-request confirmation; model text must not execute folder actions. No unrestricted computer control is authorized. Hey KAI listening is opt-in, session-only, and must visibly indicate microphone use. Keep natural speech local and require opt-in for model downloads. Update docs/TEST_WHATS_NEW.md for user-visible Test revisions.

- Voice conversations use one AudioWorklet microphone stream, stay compact, and allow follow-ups for 60 seconds after replies. During replies, require a recognized “KAI” cue to interrupt; ordinary speech must not pause playback. “KAI” alone stops the reply and leaves conversation open. Retain a question captured just after the cue, and save the interrupted turn before starting the new prompt. Assemble complete sentence audio before playing it; preserve Stop across synthesis chunks. The latest owner request replaces the default with Cute KAI: high-pitched, playful and slightly squeaky, inspired by their reference audio. Keep the prior deeper and original tones as options. Quick start plays the first complete sentence; extra buffering is optional. Keep installed voices warm only while a visible companion has voice replies enabled, with bounded idle release. Keep echo rejection, bounded transcription queuing, explicit Off/hide cleanup and no automatic OS-voice fallback. Natural-voice downloads still require an explicit setup click.

- Owner-supplied OpenAI/Anthropic API connections are private desktop capabilities. Keep encrypted keys and direct provider HTTP in Electron, behind exact trusted-window/main-frame/document IPC. Never expose them through Core/public/remote APIs, tools, earning workers, network model lists or unattended jobs. Local-Only still blocks their egress; errors must never trigger network fallback. Main chat and KAI share the provider transport, streaming cancellation and existing app-tool approvals.

- Fast Windows voices use the bundled stdin/stdout-only .NET helper and private companion IPC. Keep it offline and literal-text-only, with no shell/registry/SSML input or automatic voice installation. Apply character variations to normalized PCM, not just browser pitch hints. Preserve natural selections and never silently fall back between engines. Whole-reply playback joins prepared audio into bounded continuous sections; explain its longer initial wait. Build/benchmark the helper and verify it in the packaged Windows app when changing this path.
