name: Test installers

on:
  push:
    branches: [test]
  workflow_dispatch:

concurrency:
  group: kai-test-release
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  verify:
    if: github.ref == 'refs/heads/test'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci
        env:
          ELECTRON_SKIP_BINARY_DOWNLOAD: "1"
      - name: Install Chromium
        run: |
          npx playwright-core install --with-deps chromium
          echo "KAI_TEST_CHROMIUM=$(node -e 'process.stdout.write(require("playwright-core").chromium.executablePath())')" >> "$GITHUB_ENV"
      - run: npm test
        env:
          KAI_MASCOT_QA_DIR: mascot-qa
      - name: Verify natural voice inference
        run: node scripts/verify-neural-voice.js
        env:
          KAI_MASCOT_QA_DIR: mascot-qa
      - name: Save KAI visual checks
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mascot-visual-checks
          path: mascot-qa/*
          if-no-files-found: ignore

  windows:
    needs: verify
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci
      - name: Prepare test installer identity
        run: node scripts/prepare-test-build.js ${{ github.run_number }} ${{ github.run_attempt }}
      - name: Verify live profile handoff and credential reuse
        run: node scripts/verify-shared-profile.js
      - name: Verify native KAI companion
        run: node scripts/verify-mascot-window.js
      - name: Build Windows preflight package
        run: npm run dist -- --win --dir --publish never
      - name: Verify Windows preflight contents
        run: node scripts/verify-test-package.js
      - name: Verify Windows preflight natural voices
        run: node scripts/verify-neural-voice.js --packaged
      - name: Enable code signing (when configured)
        shell: bash
        env:
          AZ_ACCOUNT: ${{ secrets.AZURE_SIGNING_ACCOUNT }}
          AZ_PROFILE: ${{ secrets.AZURE_SIGNING_PROFILE }}
          AZ_ENDPOINT: ${{ secrets.AZURE_SIGNING_ENDPOINT }}
          AZ_PUBLISHER: ${{ secrets.AZURE_PUBLISHER_NAME }}
        run: |
          if [ -n "$AZ_ACCOUNT" ] && [ -n "$AZ_PROFILE" ]; then
            node -e "
              const fs = require('fs');
              const p = JSON.parse(fs.readFileSync('package.json', 'utf8'));
              p.build.win.azureSignOptions = {
                codeSigningAccountName: process.env.AZ_ACCOUNT,
                certificateProfileName: process.env.AZ_PROFILE,
                endpoint: process.env.AZ_ENDPOINT || 'https://eus.codesigning.azure.net',
                ...(process.env.AZ_PUBLISHER ? { publisherName: process.env.AZ_PUBLISHER } : {}),
              };
              fs.writeFileSync('package.json', JSON.stringify(p, null, 2));
            "
            echo "Code signing ENABLED (Azure Trusted Signing: $AZ_ACCOUNT/$AZ_PROFILE)"
          else
            echo "Code signing not configured — building unsigned"
          fi
      - name: Build Windows test installers
        run: npm run dist -- --win --publish never
        env:
          AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
      - run: node scripts/verify-test-package.js
      - name: Verify packaged Windows natural voices
        run: node scripts/verify-neural-voice.js --packaged
      - uses: actions/upload-artifact@v4
        with:
          name: kai-test-windows
          path: |
            dist/*.exe
            dist/*.blockmap
            dist/test*.yml
          if-no-files-found: error

  linux:
    needs: verify
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci
      - name: Prepare test installer identity
        run: node scripts/prepare-test-build.js ${{ github.run_number }} ${{ github.run_attempt }}
      - run: npm run dist -- --linux --x64 --arm64 --publish never
      - run: node scripts/verify-test-package.js
      - uses: actions/upload-artifact@v4
        with:
          name: kai-test-linux
          path: |
            dist/*.AppImage
            dist/test*.yml
          if-no-files-found: error

  publish:
    needs: [windows, linux]
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci
        env:
          ELECTRON_SKIP_BINARY_DOWNLOAD: "1"
      - uses: actions/download-artifact@v4
        with:
          pattern: kai-test-*
          path: artifacts
          merge-multiple: true
      - name: Publish test installers and isolated update feeds
        run: node scripts/publish-test-build.js artifacts
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
