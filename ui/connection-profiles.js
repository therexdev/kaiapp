"use strict";
(function(root, factory) { const p = factory(); if (typeof module === "object" && module.exports) module.exports = p; else root.KaiConnectionProfiles = p; })(typeof globalThis !== "undefined" ? globalThis : this, function() {
  return {
    googlecalendar: { name: "Calendar appointments", keywords: "calendar dentist appointment tomorrow schedule event", searches: ["calendar", "event"], guidance: "Resolve the current date, user's time zone, calendar/account and duration. Ask for missing duration or ambiguous dates. Preview exact start/end and zone. Creating an event does not book a visit with a business. Read back the returned event ID; never retry an uncertain create." },
    googledocs: { name: "Create and edit documents", keywords: "document doc write report research", searches: ["create", "document", "batch"], guidance: "Create once, preserve documentId, insert content, then read it back. Cite sources for researched facts. Use Drive to place the document in a folder. Report partially created documents with their IDs/links." },
    googlesheets: { name: "Spreadsheets and tables", keywords: "sheet spreadsheet rows table formula dentist", searches: ["create", "values", "format", "spreadsheet"], guidance: "Distinguish spreadsheet from worksheet. Describe current schemas, create once, use returned spreadsheetId for later writes, verify ranges. Use RAW for source text, keep phone numbers as text, and only use formulas deliberately requested. Preserve citations in columns or a sources tab. Drive handles folders." },
    googledrive: { name: "Folders and files", keywords: "folder file drive move organize share", searches: ["find", "create folder", "move", "metadata"], guidance: "Resolve the parent folder/account. Reuse only an explicitly selected existing folder; ambiguous names require clarification. Pass stable returned IDs, not guessed names. Verify parent membership. Google Sheets/Docs and Drive must use compatible accounts. Sending a link does not grant access: never make a file public unless explicitly requested and approved." },
    discordbot: { name: "Discord messages", keywords: "discord message dm send person channel thread", searches: ["guild members", "channel", "message", "create dm"], guidance: "Messages use the bot/app identity, not unrestricted personal-account automation. Resolve user ID and guild/channel, disambiguate duplicate names, preview sender/recipient and exact text. DMs depend on reachability. Record message ID. Provider acceptance is not evidence someone read it." },
    discord: { name: "Discord account", keywords: "discord account", searches: ["user", "guild"], guidance: "This toolkit has limited account/application actions. For messaging use Discord Bot if connected and permitted; do not claim Discord sign-in alone enables sends." },
    slack: { name: "Slack conversations", keywords: "slack message send channel person dm reply", searches: ["user", "conversation", "message", "permalink"], guidance: "Resolve workspace, sending identity and recipient/channel ID. Preview exact message and attachments/links; no guessed recipient from a duplicate display name. Preserve thread timestamp for replies. Return message permalink; uncertain sends must not auto-retry." },
    microsoft_teams: { name: "Teams conversations", keywords: "teams microsoft message send chat channel reply", searches: ["chat", "channel", "user", "message"], guidance: "Resolve tenant/account and chat/channel ID, sender and recipients. Consent and membership affect availability. Preview exact text. Use reply operation for a thread and retain returned message ID." },
    gmail: { name: "Email and drafts", keywords: "gmail email mail draft send reply contacts", searches: ["people", "draft", "email", "thread"], guidance: "Resolve exact recipient email, sending account and thread. Drafting never authorizes sending. Preview To/Cc/Bcc, subject, body and attachments. Preserve returned message/thread IDs; a timeout is not permission to send again." },
    notion: { name: "Pages and records", keywords: "notion page database records notes research", searches: ["search", "page", "block", "row"], guidance: "Find the accessible parent and current database/data-source schema. Create a page or insert typed records, retaining source links. Verify returned page/row. Avoid unrelated workspace edits." },
    github: { name: "Issues and project follow-up", keywords: "github repository repo issue bug comment pull request", searches: ["repository issues", "create an issue", "issue comment", "pull requests"], guidance: "Resolve owner/repo and issue exactly. Initial chat profile covers reading issues/PRs and creating/updating issues/comments; do not infer permission for merging, releases, secrets or repository administration." },
    todoist: { name: "Tasks and follow-ups", keywords: "todoist task project due deadline follow up", searches: ["project", "task", "comment"], guidance: "Resolve project, exact task and due date/time zone. Create/update/complete only requested tasks; retain task ID and link for follow-ups. This does not enable a KAI workflow schedule." },
    one_drive: { name: "Microsoft folders and files", keywords: "onedrive microsoft folder files workbook", searches: ["find", "folder", "move", "item"], guidance: "Resolve tenant, drive and parent folder IDs. Verify Excel account compatibility and destination. Retain IDs for partial recovery. Sharing needs its own exact recipient/access review." },
    excel: { name: "Excel workbooks and tables", keywords: "excel microsoft workbook spreadsheet table rows", searches: ["workbook", "worksheet", "range", "table row"], guidance: "Resolve drive/workbook and any required session. Create or edit typed tables and ranges using returned IDs, verify contents. Treat source text as literal data; use formulas only intentionally. OneDrive manages folder placement." },
  };
});
